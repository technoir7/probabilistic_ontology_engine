# Required for report generation (POST /v1/report/{domain}/refresh)
FIREWORKS_API_KEY=<your-key-here>
# Optional: override the default model used for reports
# FIREWORKS_REPORT_MODEL=accounts/fireworks/models/llama-v3p3-70b-instruct

# Optional: USDA NASS Quick Stats API key for higher rate limits.
NASS_API_KEY=
